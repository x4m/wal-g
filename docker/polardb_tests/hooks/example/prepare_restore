#!/usr/bin/env bash
set -Eeuo pipefail
mode=${1:?mode}
fetched_dir=${2:?fetched directory}
echo "implement: install local files from ${fetched_dir} as ${mode}, configure its PFS root, and create recovery.signal" >&2
exit 1
