#!/usr/bin/env bash
set -Eeuo pipefail
mode=${1:?mode}
fetched_dir=${2:?fetched directory}
echo "implement: install ${fetched_dir} as the ${mode} node (including polar_shared_data on a fresh PFS)" >&2
exit 1
